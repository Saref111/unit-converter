[package]
name = "convert"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.27", features = ["derive"] }
dialoguer = "0.11.0"
strum = { version = "0.26.3", features = ["derive"] }
